/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['miro.medium.com','pbs.twimg.com','avatars.githubusercontent.com'],
    },
};

export default nextConfig;
